#include <stdio.h> // Подключаем библиотеку стандартного ввода-вывода

int main() {
    // printf — это функция для печати текста в консоль
    printf("Привет! Я программирую на C в Linux через WSL!\n");
    printf("Привет! Я программирую на C в Linux через WSL!\n");
    return 0; // Сообщаем системе, что программа завершилась успешно
}